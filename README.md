### react-todoList

In order to launch the app run:
```
cd frontend
npm i
npm run-script build
cp -R build ../backend/
cd ../backend/
npm i
npm start
```
App will be awailable at http://localhost:3001
